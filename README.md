# Autoclicker

A background tool that watches your screen and dismisses OK buttons in desktop applications automatically.

Run:

```bash
uv run autoclicker --button button.png,button_alt.png --confidence 0.8 --interval 1.0
```
